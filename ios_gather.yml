---
vars:
  cli:
    host: "{{ inventory_hostname }}"
    username: ansible
    password: ansible
    transport: cli

---
# Collect all facts from the device
- ios_facts:
    gather_subset: all
    provider: "{{ cli }}"
